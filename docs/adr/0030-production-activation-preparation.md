# ADR0030 — P3 activation preparation without external activation

Status: accepted for supervised preparation only, 2026-09-13. Owner authorized exact PR13 merge through active Ruleset23161641, then a new Draft PR. No runtime provider/production values are approved.

Decision: preserve all existing domain/Gateway/schema boundaries. Keep proposed guest values solely in documentation with actual fixed-TTL/recovery/retention semantics, not executable defaults. Record live main protection separately from historical P2 missing-protection evidence. Compare provider trust/cost/operational acceptance without configuring a provider. One fixed-command Square fixture harness reuses the existing real-PG payment regression and adds transport cancellation/loopback socket evidence; no external fetch implementation or credential resolver is wired to production. Add only bounded safe diagnostics for the unexplained historical staff500; no retry/permission/test weakening.

Owner decisions join in OWNER_DECISIONS_REQUIRED.md with exact activation prerequisites and NOT_RUN real-device procedures. Readiness of the source/harness is separate from real Sandbox, hosting Request identity binding, transactional real-import writer, provider restore, dual-key rotation, legal/privacy decisions and public release. Preserve past observations and original review output. CI+static review are tied to the final head; PR stays Draft.

P3 follow-up: the original test Web launcher discarded the new safe diagnostic. Retain the first static PASS and record CODEX-P3-01 separately; forward only validated bounded structured lines, add a real authenticated HTTP/isolated-PG fault regression, and reverify the changed head. No new production feature or permission.
