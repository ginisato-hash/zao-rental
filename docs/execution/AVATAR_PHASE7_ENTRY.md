# Avatar Phase7-A RC entry

Owner authority SHA256: `dedfd26fe0db9ba47a04f8032ec1484de0e4149aeb361fcd54cffec3fbae0a57`.
Start `49d0fbc36d81025f675242a0ffd1bee0f68ce21e`; branch `codex/avatar-phase7-rc`.
Fetch/prune confirms exact Phase6/main/R15, clean tree, and continuous main → R15 →
Avatar A2/A3 → Phase4 → Phase5 → Phase6 ancestry. Branch directly from terminal
Phase6; no merge/cherry-pick/history rewrite. Phase6 branch/evidence remain frozen.

Scope: one zero-deploy targeted network reproduction using exact attempt5 only,
then retire exact Phase6 Preview deployments and exact two sensitive Preview envs.
Keep dedicated project protected, private R2 bucket/three objects, existing Neon
resource/database/0031–0032/six roles. Revoke active Phase6 R2 read credential after
Vercel retirement; no new credentials/renewal, owner DB credential, migrations,
role changes, Square/payment/real booking/Production/main merge or Phase7-B.

Network observer is test harness only. Before the first forensic navigation it
installs a route guard and task-owned CDP listener. Persist only normalized origin,
resource/initiator enums, party classification, blocked flag and count. Never save
URL paths/queries/fragments, headers, cookies, bodies, session IDs or raw CDP/stack.
Extensions retain only BROWSER_EXTENSION and count. Stop on first unexpected request,
abort it and end the flow; no speculative domain allowlist or browser retry loop.
Normal human Vercel authentication, if required, is a separate setup page in the
same one browser/context; close it before the first instrumented forensic page.
No browser cookie/profile/storage inspection or authentication bypass.

Terminal network classification is BENIGN_PLATFORM_OR_BROWSER (requires observed
metadata plus provider/browser evidence), APP_EGRESS (static source proof and only
necessary local product correction), NON_REPRODUCED, or UNKNOWN. Phase6 never becomes
PASS retrospectively. Application changes only on proven APP_EGRESS; Claude0 unless
product/runtime code changes. Required tests/build/security scan run locally.

Authoritative current results are consolidated in AVATAR_PHASE7_STATUS.json.
Next gate is separately authorized PHASE7-B protected RC Preview/targeted smoke/main
integration candidate. This Phase7-A does not deploy or merge main.
