# E07 execution scope

Current authority is the owner's explicit supervised continuation and exact PR6 merge approval.
Canonical operations/inventory/return rules remain in OPERATIONS.md, INVENTORY_RULES.md,
RETURN_RULES.md and the E06 contracts; ADR 0014 records E07 implementation choices and limits.
TASKS.json E07 is an original proposal. Customer return portions stay in E12, not completed here.

Deliver: one new Draft PR with 0005, transfer service/protected API/UI, explicit store permissions,
real-PG upgrade/regression/competition tests, normal password UI/API/DB tests, CI and static Claude
review (initial + at most two rereviews). Each review records exact base/head and snapshot hash.
No E07 merge, E08+, Square, production, actual-data import, outside-network publication, daemon,
PR3 adoption, Runner activation or new isolation services. UNATTENDED_HOLD stays unchanged.

Acceptance evidence distinguishes real PostgreSQL, real synthetic password sessions, controlled
operational time, dropped transport responses, viewport simulation and unimplemented customer
lending/return. An affected promise must remain visible, not become a silent cancellation/refund.

Finite budget started 2026-09-11T15:38:34Z; hard stop 2026-09-11T19:38:34Z. Do not extend.
On completion, deadline or required human action, save partial work/evidence and stop only owned
Web/browser/PostgreSQL resources. Authentication/quota/billing uncertainty stops review without
another provider/key or automatic retry. Final results live in PR comments and per-head manifests.
