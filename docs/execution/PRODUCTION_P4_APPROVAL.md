# Production Activation P4 — direct Owner authority

Adopted 2026-09-13T13:42:25Z / 22:42:25 JST. The Owner authorized exact PR14 merge,
then a fresh P4 worktree, code/tests, Draft PR and existing Team/credits OFF/tool-free
independent static review. Historical P3 reviews (2) remain historical; no old unused
budget is added. P4 begins with zero review starts. No approval to merge the new PR.

Owner decisions:
- Guest draft/checkout BALANCED: context3600, absolute86400, recovery43200, replay600,
  retention86400, window60, peer180, global1200. Confirmed booking access is a separate
  high-entropy, hash-stored, revocable capability through original due; no raw token in
  URLs/logs/analytics. No guest/HOLD TTL extension. Email provider still unconnected.
- Vercel direct is the first hosting candidate; no fronting proxy. Trusted ingress
  remains fail closed until actual Vercel verification.
- Square SANDBOX only: application, synthetic tokenization/create/webhook/lookup,
  <=20 synthetic payments and <=5 Sandbox refund trials. No production/cards/merchant;
  UNKNOWN never permits a different charge key. Keep fixture and actual proof separate.
- R2 first candidate adapter proof; no contract, paid plan or production credential.
- Backup selection targets RPO<=5min/RTO<=4h/30days/monthly plus important migrations;
  off-host+encryption+PITR required, not an achieved provider SLA.
- First launch independent coupon OFF; existing prior-day15:00JST paid 5% eligibility
  remains. Tax, sale dates, cleaning time, NAP, final legal wording, rights, real stock,
  devices, domain/deployment/search integrations remain Owner gates.

## Subsequent Owner metadata answer (same authority, no secrets)
Existing Sandbox application: UNCONFIRMED; do not assume one exists.
Approved secret store: UNSELECTED / UNCONFIGURED.
Sandbox HTTPS webhook receiver: NOT CREATED.
These actual external connections are BLOCKED. Continue independent code, fixtures,
real dedicated PostgreSQL and preconnection verification. Ask only when the actual
credential/storage selection/public webhook setup is necessary. Never request or
save token/signature/card values in chat/repository/logs.

## Boundaries
Preserve existing business rules and chargeReady=false. Production, real people/stock,
real Square without the missing prerequisites, new billing/privilege, PR3/Runner,
Ruleset changes, public release and new-PR merge are not authorized. No new daemon.
Finish with exact-head CI/review, honest external NOT_RUN and owned-process cleanup.
