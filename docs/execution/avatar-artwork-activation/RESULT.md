# Phase5 artwork activation — implementation result

Implementation/local acceptance PASS; independent review is pending. This is not yet a
final CASE A claim. Base01c225eaed80eb5ee95e22d1ece9eef666aa22e3; new branch
codex/avatar-phase5-artwork-activation. Direct Owner authority is saved verbatim and
SHA256-bound. Existing Phase5 code PASS/history and all0001–0032 bytes are preserved.

Three original locally authored derivatives were completed from the accepted source
references: appearance-1.webp800x2000 (orange jacket/dark pants), appearance-2.webp800x2000
(light jacket/dark teal pants), generic-ski.webp160x2000 (unbranded neutral ski).
No external/image-model assets were used. Internal scripted vector drawing is rasterized
by the existing Sharp toolchain; only alphaWebP is imported/served. These are explicitly
Owner-authorized new illustrations, not the old striped fixture or faithful product
photographs. Both body silhouettes are byte-identical alpha masks, with the same neutral
pose/landmarks; clothing palette is the only appearance distinction. No exact product,
model, gender/body-size, eligibility, recommendation or price meaning is introduced.

The authority delegates local derivative approval on spec/manifest validation. Each final
SHA has approvedByOwner=true, OWNER_AUTHORITY_THIS_DOCUMENT, AVATAR_VISUALIZATION_V1,
GENERIC_REFERENCE and OWNER_SUPPLIED_SOURCE_DERIVATIVE_LOCAL_USE_ONLY. ProductionApproved
is false. This states the local rights basis; it does not independently certify source
copyright ownership or Production licensing. Original opaque120px sources remain outside
Git/import/delivery. Their six hashes and historical intake are preserved.

Raster checks: exact dimensions, staticWebP, metadata absent, alpha/transparent pixels,
physical head/sole or tip/tail at rows0/1999, no external references, horizontal clear
margins, existing validAvatarRaster PASS. Parent visually inspected all3 derivatives and
mobile/desktop screenshots; no wall/floor/shadow plate/text/logo was authored into artwork.
The renderer's existing floor guide is HTML/CSS, not a floor in the image. Optional separate
BOOT/JACKET/PANTS are null. The clothing/boots drawn in the full-body illustration are not
separately published overlay layers.

The local helper accepts only the approved manifest/hash/authority set. It refuses
Production mode, ambient DB config and any Pool outside this worktree's owned loopback
identity before connecting. It rechecks server identity, locks content tables, requires
fresh empty content and inserts media/revision/outbox/current release/purpose grants and
new visual UUIDs atomically. There is no overwrite, upsert, existing binding retarget,
web import endpoint or hosted credential. A second import is rejected without row changes.
The existing internalOnly=false field permits local guest delivery; local-only enforcement
comes from the guarded importer and unchanged Production-disabled runtime, not that field.
Each local import uses a finite24-hour rights window. The owned local cluster closes after
acceptance; ACTIVE will describe the proved milestone, not an unattended running service.

Final counts and hashes are in validation.json. GuestBooking uses the unchanged real UI,
guest auth and media routes with two synthetic members170/180cm and saved business lengths.
Both appearances and3directions at390/1440px produce24DOM measurements, all within the
existing1px scale/floor tolerance. Cross-guest/anonymous/stale member/revision/logout and
rights-revoke denials pass. Optional/no eligible art never blocks ordinary candidates.
No renderer-triggered business POST or new metadata fetch; guest/business rows unchanged
through visual interactions. Existing idempotent guest context navigation is excluded
from visual POST counts. Initial ordinary user preview is setup, not an extra visual call.
No actual booking/payment occurs. This is Chromium viewport coverage, not physical Safari
or hosted/remote CI acceptance. Local test harness uses existing fake ports only.

AV-1: all13 imported binding UPDATE fields rejected. PHASE4-1: all3staff permissions allow
bytes and the ordinary protected preview entry; BOOKING_VIEW-only denies both. AV-3:
actual dedicated Avatar role denied underlying content/workspace/business/credential reads,
mutations and DDL; pinned function, no PUBLIC execution or elevated role privileges. Its
historical general content_read and future Production scope are unchanged. AV-2 retains
no-art/no-fake behavior and approved core art now exists; independent disposition pending.
PHASE5-1 rate-limiter absence remains LOW/local-only, no nonlocal activation authority.

Hosted Neon/Vercel/R2/Square/Production/public deployment/real payment/real booking/main
merge/external browser/image search0. GitHub and the authorized static Claude review are
separate control-plane operations. No Phase6 work. All owned PG/Next/Chromium resources
close after tests. Final independent verdict, reviewed SHA and documentation-only receipt
will be recorded before the CASE A/B/C stop.
