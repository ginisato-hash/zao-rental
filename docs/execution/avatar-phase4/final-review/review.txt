{
  "branch": "codex/avatar-phase4-customer-renderer",
  "base_sha": "96a34aee8dccb66dca11199af29c33aaf1127546",
  "head_sha": "790b809a8d8a8989594e0982e264b3a220527936",
  "authority_sha256": "3ee51a2775abf98fd5943fc441639997e5f6c69e0e98a04d6cbbf16c986715e1",
  "snapshot_sha256": "9282642d664e4d02a813f1642a9ca3aa3575ef82dbd0c4997f9b35548c3e3153",
  "verdict": "PASS",
  "tests_executed": false,
  "scope_assessment": "Reviewed the full unified diff against base 96a34ae: new avatar-media route, staff preview page/component/CSS, avatarLayout pure layout engine, avatarDerivative/validAvatarRaster media gate, loadAvatarPreview server loader, the avatar-visuals.ts findForDelivery/eligible SQL addition, and Phase4 test fixtures/E2E. Confirmed no route/file outside this set is touched: GuestBooking.tsx, the existing /media route, public-content/public-runtime, staff-runtime, all 0001-0031 migrations, recommendation/hold/quote/payment/booking core packages, and content-roles.ts provisioner are byte-identical to base per the diff and migration-hashes.json. Traced control flow by hand (not log-trusting) through avatarMediaHandler -> avatarDerivative -> PostgresAvatarVisuals.findForDelivery -> the single eligibility SQL statement, and through the /preview/avatar page -> loadAvatarPreview -> authorizeBookingActor -> mapAvatarVisualization -> avatarLayout -> AvatarFitPreview. Independently recomputed the physical-ratio geometry for the 1440px/153cm and other recorded browser measurements from the layout formulas and confirmed they match the committed browser-measurements.json to within floating-point rounding.",
  "findings": [
    {
      "id": "PHASE4-1",
      "severity": "LOW",
      "title": "Avatar byte-delivery route accepts a lower permission bar (BOOKING_VIEW only) than the staff preview page that is supposed to gate it (BOOKING_VIEW+HOLD_VIEW+QUOTE_VIEW)",
      "reviewed_sha": "790b809a8d8a8989594e0982e264b3a220527936",
      "file_or_component": "apps/web/src/lib/avatar-media-http.ts:4 vs apps/web/src/app/preview/avatar/page.tsx:14",
      "line": 4,
      "counterexample": "A staff principal holding only BOOKING_VIEW (HOLD_VIEW/QUOTE_VIEW explicitly denied) is blocked by page.tsx's `.every(...)` check when loading /preview/avatar, but avatarMediaHandler's `allowed()` only tests `permissions.includes('BOOKING_VIEW')`. If such a principal obtains a visualId+sha256 pair from any out-of-band source (browser cache/history, a shared screenshot, a colleague's open tab on a shared terminal), GET /avatar-media/<visualId>/<hash> succeeds for them even though they cannot reach the page that is meant to be the sole legitimate path to that URL.",
      "impact": "Low: visualId/sha256 are not enumerable (UUID + 64-hex digest) and are not linked from any UI a BOOKING_VIEW-only principal can reach; the disclosed bytes are a generic or model-bound reference image, not PII, price, or booking data. Still, the two layers of the same feature enforce different authorization boundaries, which is a latent inconsistency if a future change (e.g., a listing/search surface) makes visualIds discoverable.",
      "test_coverage": "e2e.log/local-e2e.json test 'staff without required read permission is denied for preview and bytes' revokes BOOKING_VIEW itself and shows both surfaces deny together; no test isolates a principal with BOOKING_VIEW granted but HOLD_VIEW/QUOTE_VIEW denied to show the asymmetric result.",
      "minimum_safe_correction": "Either document that BOOKING_VIEW alone is the intended minimum for viewing rental-illustration bytes (a deliberate looser bar than the full preview page), or align avatarMediaHandler's `allowed()` to require the same permission set the preview page enforces.",
      "required_proof": "A local PostgreSQL/browser check with a principal holding BOOKING_VIEW only (HOLD_VIEW and QUOTE_VIEW explicitly overridden to false) asserting the actual GET /avatar-media/... status, turning this into an explicit tested decision rather than an implicit gap."
    }
  ],
  "historical_finding_dispositions": [
    {
      "id": "AV-1",
      "original_severity": "LOW",
      "status": "OPEN",
      "reason": "Unchanged in this diff: avatar_visuals rows remain field-mutable beyond id/created_at at the schema level (0031 untouched); no writer/editor route was added in Phase4. Read-time purpose-grant containment in PostgresAvatarVisuals.eligible() still makes any retargeted row ineligible at delivery, so the residual risk stays latent, not exploitable, exactly as previously recorded."
    },
    {
      "id": "AV-2",
      "original_severity": "LOW",
      "status": "RECORDED_PRODUCT_DECISION_NOT_INDEPENDENTLY_CLOSED",
      "reason": "Phase4 makes the explicit product choice documented in AVATAR_ARTWORK_SPEC.md section 12 and enforced by mapAvatarVisualization's final all-or-nothing gate: zero artwork anywhere yields no renderer and no ratio-only fallback, confirmed by unit test 'absent invalid and zero artwork do not invent renderer or images'. This is a scoped decision for Phase4, not a claim that the original LOW is independently closed, consistent with ADR 0033 and FINDING_DISPOSITION.md wording."
    },
    {
      "id": "AV-3",
      "original_severity": "LOW",
      "status": "OPEN",
      "reason": "The synthetic local content_read test role in tests/avatar/phase4-e2e.ts is granted additional SELECT on avatar_visuals/content_revision_records/content_outbox and EXECUTE on avatar_visual_variant_matches, strictly inside the test fixture (scripts/content-roles.ts, the real provisioner, is unchanged and grants nothing new). No route in this diff returns content_workspace JSON to any client. Privilege-minimization for a future hosted role remains unresolved and is correctly left OPEN."
    }
  ],
  "severity_counts": {"BLOCKER": 0, "HIGH": 0, "MEDIUM": 0, "LOW": 4},
  "insufficient_evidence": false,
  "physical_rendering_assessment": "avatarLayout (packages/core/src/avatar/layout.ts) is a pure function with no IO. It validates every VisualRef (uuid/digest regex, match enum, normalized anchor/position, and anchor.y===position.y specifically for AVATAR/SKI to preserve the common floor) before using it. Ski height is computed as ratio*1000 and width as ratio*80, i.e. the ski artwork is uniformly scaled in both axes by the same ratio, never stretched, and both AVATAR and SKI boxes share y bottom=1000 canonical units, giving the required common floor. I independently recomputed the 1440px/153cm case from these formulas (bodyX=0, ski width=72, ski x=592, stage width=744+80 margins=... normalized to 352px stage) and it reproduces the recorded browser-measurements.json values (body left ~18.92px, ski left ~299px) to sub-pixel precision, corroborating the reported max ski-height error of 0.0125px and max floor-difference of 0.015625px across all 12 recorded viewport/direction combinations, comfortably inside the ±1px authority tolerance. The stage bounding box is the union of all placed layers plus a fixed 40-canonical-unit margin on every edge, then expressed entirely as percentages of one aspect-ratio-locked container, so any CSS/viewport scaling is uniform and cannot silently shrink the ski relative to the body (verified: same-length ski/body ratio holds at 320/390/768/1440px). One implementation quirk, not a defect: the ski lane's horizontal placement formula (bodyX+400+120+w+offset) inserts a gap that grows with the ski's own canonical width in addition to the fixed 120-unit margin; this only affects unused whitespace between body and ski, not any measured ratio, and is consistent with every recorded measurement. Missing avatar/boot/jacket/pants/ski each independently omit only that layer (unit tests 'missing X omits only that layer'); zero visualization renders nothing (empty string from AvatarFitPreview, and AvatarCustomerPreview shows a text-only 'no artwork' status) — no fake silhouette or product is ever synthesized, confirmed by explicit unit and browser tests.",
  "media_security_assessment": "GET /avatar-media/[visualId]/[hash] (avatarMediaHandler) rejects non-GET (405+Allow), any query string, malformed path segments, and non-conforming UUID/SHA256 shapes before touching auth or DB. It checks staff auth+BOOKING_VIEW once before DB work and again after byte read/validation, rejecting on any session-stamp change (fail-closed, 404, no distinguishing signal). avatarDerivative (packages/core/src/avatar/media.ts) reads eligibility (findForDelivery) before reading bytes, recomputes SHA256 of the actual stored bytes and requires it to equal the URL-supplied digest (the URL hash is only ever used as a comparison value, never as a lookup key beyond the visual's own DB-resolved digest), runs validAvatarRaster (RIFF/VP8X/VP8/VP8L/ALPH-only chunk walk, 4MiB/1.6Mpx/height<=2000 bounds via Sharp, exact SKI 0.08 / other-layer 0.4 aspect check, alpha presence, no EXIF/ICC/XMP/IPTC, and physical top/bottom touch for AVATAR/SKI), then re-checks eligibility a second time and requires canonical equality with the first read before releasing bytes — directly implementing the 'reauthorize around byte IO' requirement, not merely claimed by a log. The eligibility SQL (PostgresAvatarVisuals.eligible) independently enforces ACTIVE state, exact visualId match, current release membership (release.entries containment AND workspace.catalog.current equality), revision-record identity by comparing the mutable workspace-JSON copy against the physically-immutable content_revision_records payload (this is how a JSON-level tamper of the 'immutable' revision fails closed even though the workspace row itself is technically writable), explicit AVATAR_VISUALIZATION_V1 purpose-grant containment keyed on every binding field, media processed/rightsConfirmed/non-internal/rightsUntil-future, and digest binding via media.immutableSha256=visual.derivative_sha256. Responses never carry JSON, only raw image bytes with Cache-Control: private, no-store, CSP sandbox, nosniff and noindex; all denial paths return an empty 404 body indistinguishable across malformed-input, wrong-visual, wrong-digest, revoked-rights, and disabled cases (non-enumerable, matching authority section 14/16). Next Image is used with `unoptimized`, so /_next/image is never invoked for these assets (confirmed both by direct code inspection and by the 'no _next/image count' test), avoiding a public-optimizer cache path for rights-sensitive bytes.",
  "authorization_business_assessment": "loadAvatarPreview (packages/core/src/avatar/preview.ts) calls authorizeBookingActor three times: once for base permission (BOOKING_VIEW+HOLD_VIEW+QUOTE_VIEW), once with the row's actual pickupStore/returnStore after the owner-scoped SELECT (`WHERE id=$1 AND owner_id=$2`), and once more after the optional visual-metadata read (wrapped in its own try/catch that does not swallow authorization/business errors, matching the same before/after pattern used in withAvatarVisualization and the content release-plan reauthorize hook). /preview/avatar/page.tsx additionally re-validates the staff session stamp after the loader returns and 404s on any change, strictly rejects any query parameter outside {preview,member,locale}, and validates preview/member shapes before touching the DB. No route in this diff calls RecommendationService.preview/select/resume, and mapAvatarVisualization/avatarLayout are both pure, IO-free functions verified never to mutate their MemberRecommendation/Profile inputs (unit test 'deterministic; no mutation or business callbacks' does a structuredClone-before/after equality check). The Phase4 E2E harness asserts zero non-auth POSTs and zero external requests throughout every browser scenario including appearance/direction switching, confirming the renderer causes no business write. GuestBooking.tsx is unmodified in the diff and is asserted at test time to not reference AvatarFitPreview; the existing /media/[hash]/[file] route and public-content/public-runtime files are untouched; all 31 migrations are unchanged by hash. The only new DB write surface (avatar_visuals) already existed from A2/A3 and gains no writer/editor path here — the sole code change to avatar-visuals.ts is the additive `findForDelivery`/private `eligible` refactor, which is read-only.",
  "validation_assessment": "Self-reported counts (235 unit / 71 real local PostgreSQL / 27 browser, all PASS/0 fail) are Codex self-validation and were not re-executed by me, consistent with the tools-disabled static-review constraint. However, I did not merely trust the PASS logs: I read tests/avatar/phase4-e2e.ts and tests/unit/avatar-renderer.test.ts in full and confirmed each named check actually exercises the claimed condition (e.g., the ten 'after metadata load denies next GET' cases genuinely mutate a cloned ContentWorkflowState and restore it after each assertion; the ratio/bounds assertions in the Playwright script read real getBoundingClientRect() values and compare them against the expected length/170 ratio with a literal <=1 px tolerance, matching the persisted browser-measurements.json). Migration hash equality was verified by direct comparison of migration-hashes.json against the 0031 file content shown in this snapshot. The one artifact I cannot verify without execution is that the actual CI run produced exactly these log files unmodified; that is an inherent limit of static review and is treated as accepted residual risk under this milestone's explicit 'tests_executed:false' scope, not as missing evidence, because the underlying source fully explains and reproduces the reported numbers.",
  "next_gate": "AVATAR PHASE 5 — GUEST BOOKING INTEGRATION + REAL ARTWORK ACTIVATION. This PASS authorizes only evidence persistence, commit/push/readback and STOP. No Phase5 work, no real artwork creation/upload, no GuestBooking customer insertion, no public/customer activation, no main merge, and no deployment are authorized by this review."
}
