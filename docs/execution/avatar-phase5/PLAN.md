# Phase5 implementation plan and interpretation

The direct Owner adoption message is saved verbatim in AVATAR_PHASE5_AUTHORITY.md.
No separate detailed Phase5 authority was found in the current attachments or GitHub
checkout at start. Owner has been asked asynchronously for a separate document if one
exists; independent work proceeds under the explicit adoption scope, not invented authority.
Phase4 receipt4485966 is frozen; do not repeat its acceptance/review or reuse its review guard.
Parent is the single writer. Work on codex/avatar-phase5-guest-integration only.

1. GuestBooking candidate step reuses AvatarFitPreview. Load both appearances from an
   owned saved recommendation using read-only GET; appearance switches remain local.
   Existing length radios remain the only selection state. No visual callback to business
   writes, recommendation recalculation, HOLD, quote, booking or payment.
2. Guest image URLs bind the current guest draft/revision/member. Resolve ordinary guest
   context, recheck current ownership and offered references around byte IO. Always
   reauthorize current rights/release/digest. No public bearer token or browser secret read.
3. Add0032 (never edit0001–0031): immutable visual binding fields; a narrow eligible-metadata
   view plus current-eligibility derivative read function. Dedicated local Avatar reader
   can read neither full workspace nor raw media objects. SECURITY DEFINER uses pinned
   search_path, qualified identifiers and revoked PUBLIC execution. No writer/editor API.
4. Address PHASE4-1 by aligning staff byte delivery with the same three read permissions
   as preview, with a BOOKING_VIEW-only negative test. AV-1/AV-3 receive local proof;
   AV-2 retains zero-art/no-renderer as the explicit product decision. Original findings
   and severity remain; independent review must assess corrections.
5. Synthetic fixture-only unit/local PG/browser proof, scoped existing regressions,
   migration hashes, lint/typecheck/build, secret/diff scans. No approved real artwork
   is currently known. Synthetic fixtures never count as approved customer artwork.
6. After implementation/evidence commit/push/readback, one fresh static Claude initial
   review (Team, extra OFF, tools/MCP/hooks/browser/provider/edit/push disabled). Follow
   permanent governance for any necessary safety correction and re-review, without
   carrying forward Phase4's used review budget or retrying unknown/quota failures.
7. Save final review/dispositions/receipt, push/readback/clean. With no approved artwork,
   normal terminal is PHASE5_CODE_PASS_ARTWORK_INPUT_REQUIRED; no deployed activation.

Hosted Neon, Vercel, R2, Production, main merge, new PR, real customer/payment,
Square/provider replay, email/SMS and new service operations remain0. Authorized GitHub
branch synchronization and Claude static review are separate control-plane activities.
No new graphics dependency, 3D, canvas/WebGL, remote artwork creation, or upload.
