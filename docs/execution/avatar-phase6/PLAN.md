# Avatar Phase6 execution plan

1. Pin remote base and authority; preserve completed branches and all migration bytes.
2. Implement bounded local artwork import, purpose-separated Avatar throttling, strict hosted
   runtime and private R2 byte delivery; verify unit/local PostgreSQL/browser regressions.
3. Read existing provider state; prepare exact least-privilege roles, one-shot0031/0032 runner,
   synthetic fixture/artwork import and exact3 R2 uploads. Durable dispatch guards precede writes.
4. Build/deploy a protected Preview (max2), prove hosted guest flow, rights and ownership
   failures, abuse budgets, mobile/desktop physical scale and actual private-provider bytes.
5. Remove setup/write credentials and failed resources; retain only accepted protected Preview,
   scoped runtime credentials and private derivatives. Review exact implementation once with
   tools-disabled Claude Team; correction only if severity requires. Record receipt and stop.

Assumptions: existing R15 synthetic records are preserved; new acceptance data is explicitly
Phase6-marked. Existing business rate policy remains unchanged. Avatar metadata/media use
separate budgets on the existing rate table. No artwork regeneration or source-photo upload.
Runtime setup-owner credentials are forbidden. Current role/DB/TLS identity is verified.
Implementation validation and independent review remain separate claims.
