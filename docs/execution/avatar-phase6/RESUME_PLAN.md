# Phase6 continuation

Resume the retained partial implementation at71ae7a5; do not replay prior successful proof
unless dependent source changes. Owner authority preserves STOP_RECORD and old status.
No historical browser output/URL reconstruction, no global inventory, no credential rotation.

1. Commit/push/readback this amendment and containment record before hosted work.
2. Verify existing Neon identity/history; source-pin0031/32 and use one durable migration guard.
   Complete local tests for new runner/role/import code before dispatch. Use scoped setup
   credentials only in the local operator; no setup credentials in Preview.
3. Prove actual hosted least privilege and separate Avatar/global budgets. Preserve R15 data.
4. At R2 step, inspect authorized API/CLI availability, then only if needed request normal login.
   Reuse/create max1 private bucket and put exact3 approved derivatives with separate credentials.
5. Create max1 dedicated zao-rental-avatar-preview project with Authentication ON and no domains,
   no Production deployment/alias, no Square/payment/refund/webhook env. Inject only explicit
   branch-scoped Preview runtime config and Phase6 mode. Verify key metadata, never env values.
6. Deploy max2 protected Previews; prove guest flow,390/1440 dimensions, ownership/rights,
   logout and abuse budgets. Cleanup setup/write credentials, then exact final Claude review.
7. Report with historical incident annotation and stop before Phase7.

The dedicated project id will be pinned after create/readback; configuration must fail closed
until then. Browser logs/evidence emit only safe classifications and task-owned IDs/origins.
