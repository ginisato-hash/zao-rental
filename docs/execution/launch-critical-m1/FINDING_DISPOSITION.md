# M1 independent review disposition

Initial review of 40f6be236d883083d5ae64aa5294a4a693d0593e: BLOCKER0/HIGH1/MEDIUM1/LOW2; static only, no reviewer tools or test execution. The correction review is reserved for this patch only.

- F1 HIGH: fixed. An active OUT pole loan previously prevented all quantity adjustment. `ops_reconcile_poles` runs as the existing restricted custody executor, validates the maintained session, both inventory permissions, store, exact stocktake revision/baseline, observed integer count, unchanged OUT count and every active slot/transfer promise. A private transaction/pid/exact-change effect admits only this adjustment through the existing stock guard. All previous guard branches remain. No fake receipt, owner bypass, relocation or loan mutation. Existing ledger audit records actor/reason/before/after. Tests prove eight physical pairs plus one OUT pair reconcile from ten to nine; the OUT loan stays byte-for-byte unchanged. Replays do not adjust twice; stale/direct effect forgery and reduction below a future nine-pair promise are denied.
- F2 MEDIUM: explicit store arguments added to the charge permission check and wrong-store dispatch/lookup regression added. The claimed empty-store bypass was not present: `row()` already called `booking()` which requires BOOKING_VIEW at both immutable booking stores; permissions share one staff store scope, and transaction/SQL financial guards also check both stores. The defensive explicit RENTAL_AMEND check now expresses that same contract directly. No redundant acting_store column or weakening to pickup-only scope was introduced. The wrong-store actor reaches neither create nor lookup and the financial row is unchanged.
- F3 LOW: recorded. Boot pair printing is an optional toggle; ski two-label same-ID and board one-label behavior remain as authorized. Physical printed-label acceptance remains separate.
- F4 LOW: recorded, grant retained for a concrete current use. The same operations role supplies normal CustodyService.prepare/checkout, which updates allocation_stage at custody-service.ts lines48/62. Existing guarded stage transitions and the normal browser/custody regression require this column grant; AmendmentService is not its only caller.

Migration0033 is the new, unmerged, local-only migration in this PR; its correction remains in that proposed migration. Historical migrations0001–0032 stay byte-identical. No hosted migration has been applied.
