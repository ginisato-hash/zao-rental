# M1.5 independent review disposition

Initial static review on15cf6c2ba9d9152f639a5eb6ae62acbc5ef02f3d: CHANGES_REQUIRED, BLOCKER0 HIGH1 MEDIUM1 LOW2. Model starts1; tools/MCP/hooks/browser/provider0; Existing Claude Team extra usage OFF. One pre-dispatch automatic approval rejection was resolved by fresh proof that the repository/exact commit/tree and every snapshot source were public GitHub content and by the Owner M1.5§14 authority. It did not consume a model start or cause a duplicate send.

- F1 HIGH: payment OFF skipped validation but returned the supplied gateway. Fixed by retaining a local validatedPayment only after all enabled-flag provenance/credential checks succeed. The return object never reads an unvalidated input.payment. A supplied Sandbox gateway/throwing credential resolver under payment OFF produces runtime.payment=null and zero resolver/transport calls. Current HTTP checkout gates remain unchanged; the prior unvalidated port was not reachable from routes.
- F2 MEDIUM: media was implicitly coupled to guest booking through publicRuntime. Fixed by independently activating only content_read for a media-only runtime and by using publicContentPool for catalog/pages/derivatives. The normal media route delegates to a shared release/rights handler; it does not need a guest or staff runtime. Real-PG proof enables media with booking/recovery/staff OFF, connects only content_read, returns matching bytes from the private R2 fixture, denies on provider failure, and rechecks rights both during and before later reads. Media OFF remains404. No config capability was removed to hide the failing combination.
- F3 LOW retained: the legacy config.authSecret shape contains the guest root for the shared dev-facing configuration shape, while staff auth receives staffKey directly. Production access/recovery routes return their explicit runtime services before any legacy derivation; independent accessKey/recoveryKey validation remains enforced. No Production path derives these keys from config.authSecret. This naming debt is recorded, not conflated with a current key-sharing defect.
- F4 LOW retained: TLS proof includes pinned pg/Node internal socket fields. Missing/changed fields fail closed. Node24.15.0/pg8.23.1 are pinned; a version upgrade must revalidate the TLS connector rather than remove checks. No actual Neon connection is claimed here.

Correction validation and the one conditional correction review are recorded separately. The initial result and exact snapshot hashes remain immutable; no third review or LOW loop is authorized.
