# P1 verification and limits

Final local verify ran43 commands, all exit0, including reference27, lint/typecheck/unit, real PostgreSQL, build and browser/E2E; commands.json records timestamps and individual logs. Mac-only sandbox regression makes ZERO model calls and does not activate Runner. CI later runs the same suite minus that Mac-only command.

New P1:12 real PostgreSQL/API guest cases,3 ordinary browser/API/PostgreSQL recovery checks,11 unit adapter/import/gate/media cases. Asset camera component6 cases (4existing+2new), synthetic MediaStream only. Guest recovery UI: saved preview -> server-generated hashed recovery code -> lost response after real commit -> owned Web stop/restart -> same-tab reload/replay -> same preview/owner/expiry, one recovery audit, no HOLD/payment. Confirmed booking recovery is real-PG/API with isolated synthetic payment, not that UI test. No recovery secret or cookie is captured in logs/screenshots. Width320/360/390/430/768/1024 with touch emulation is not real hardware.

Preserved failures: first fullverify stopped at readonly NODE_ENV test typings (fixed using Reflect only in test/finally restore). Second fullverify passed through new12PG cases but failed at page.reload while Next HMR was reconnecting across an intentionally killed/restarted owned Web process. The test now navigates the same tab to about:blank before restart, then back to the ordinary booking page after health readiness. This prevents development HMR navigation racing Playwright while preserving sessionStorage requestUUID; no secret is stored/injected and no business assertion/retry/timeout inflation was added. Focused test then passed, followed by fullverify. Raw failures remain in outer outputs/production-p1; no success is claimed for those runs.

Before finalverify, Square deadlines were strengthened to settle on ignored cancellation; source failure is masked and never POSTs. Future real transport must still honor AbortSignal for resource termination. No actual Square/credentials/webhook request occurred. Real provider callback/tokenization and transport cancellation stay unconnected. Storage/import tests are fixtures and planner validation, not real provider/import execution. Existing PG content release/rights/rollback and SQL privilege tests remain in verify.

No existing migration0001-0020, price JSON, Runner policy or workflow was changed. Additive0021 is tested only against owned isolated development PostgreSQL. Production noindex/chargeReady=false/restricted composition remain. No full-production readiness claim: see checklist and pending launch-gates. Existing A-G/intake/cm/HOLD-continuation/transfer/quantitywear/payment/custody/MULTIDAY later-day regression remains in verify. Performance logs retain explicit operation/fixture/environment, not fieldCWV or production SLA.
