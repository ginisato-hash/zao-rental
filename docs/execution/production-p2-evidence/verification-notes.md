# P2 verification evidence and limits

Local verify44 commands exit0 (CI43: excludes only the Mac-only sandbox boundary test, which makes zero model calls). Includes original27 reference tests, lint/typecheck/unit, build, all actual-PG/auth/HOLD/price/recommendation/A-G/payment/custody/transfer/wear/late-pickup, public SSR/SEO/UI and E2E. Existing business rules and migrations0001–0021, price JSON and SquareSandboxGateway were not edited. No Runner/model/production activation.

New14 unit cases: config/schema/env and exact approval, spoof-proof dispatcher contract/canonical peers, concrete injectable Sandbox transport/UNKNOWN/no retry/signed webhook/current lookup/money/cancellation, provider private media/rights/ticket/purge/deletion gates, staged source import/explicit dry-run, structured event sink and secret lifecycle. No real provider calls/credentials.
New6 actual-PG/ordinary-HTTP composition cases: missing/unapproved/invalid configuration, two replica startup audits with immutable DB policy, spoofed ingress rejected without context writes, trusted synthetic dispatcher allows context/draft while simulation still fails under production handler mode, separate PG pools share exactly3 peer/5 global synthetic budgets, expiry/recovery/same-request replay then revocation. Values are synthetic, not owner-approved production settings. Normal apps/web production composition remains unconnected. Existing P1 real-PG/UI recovery/retention/revocation regressions retained.

New4 actual cold-cluster backup/restore cases: owned pools stop first; per-file manifest/checksum; corrupt manifest fails before start; fresh restored directory has same81 public tables/180 rows, migration checksums and same Postgres version; migration replay and failed transactional DDL leave all row digests unchanged. See restore.json for run-specific hashes/timing. RPO0 means synthetic quiescence, not production SLA. RTO includes restore and checks, not off-site retrieval or full-size production recovery. Physical files include synthetic authentication material and are excluded from Git/CI/review. No production DB/backup provider, pg_dump install, hot file copy or cross-major restore.

QR8 cases now include visible slow acknowledgement, one callback while pending, touch input and reload without invented persistence, alongside camera denied/missing/offline/reconnect/unmount/duplicate tests. Synthetic canvas camera, Chromium hasTouch and320/360/390/430/768/1024 widths are not real iPhone/Android/tablet/camera/CWV. Actual server-persisted return cycle/duplicate/reload evidence is the unchanged custody UI/PG suite, not the input-only fixture's reset behavior.

Initial failed checks are retained in outer outputs/production-p2: lint/typecheck first invoked from the outer non-repository directory (no tests ran); corrected typecheck found optional RequestInit.body and inferred UUID-default typing, both fixed. First restore attempt refused copying into an already existing target, and test cleanup attempted an already closed pool; fixed by absent child targets under unique parents and once-only pool/cluster lifecycle. No force-overwrite, retry/skip, timeout inflation or business assertion weakening. Focused restore then passed, followed by this full verify44.

No product migration or broad role changes. Media provider/CDN and import commit are interface/dry-run; structured sink/secret metadata are fixture contracts, not actual alerts/rotation. Real ingress, production policy values, actual Sandbox, private storage/CDN, real Salomon/stock/photos, devices/fieldCWV, commercial/legal copy/tax/season/coupon/cleaning/NAP/rights, final RPO/RTO/domain/deployment, main protection and release approval remain explicit gates. Existing main protection remains false; no GitHub settings changed. Performance logs identify their operation/fixtures; they are not production claims.
