# PROD-R2B — backup automation mechanism (code-only, inert)

Authority: PR #26 comment `5762217012` ("Technical Director authorization — PROD-R2B backup automation implementation"), under `5760967760` → `5760694824` → `5760102773`. Revised per PR #27 review `5763014617` (REQUEST_CHANGES, findings R2B-F1–F7) and its cited clarification `5762567427`.

Base: `main` `c1c438dcb2cf3f29acc39cfb451c49419f414fda`, tree `f1dbdb017b9b9e0cc04b63a1acb54e76cd2d31f4`. Branch `claude/prod-r2b-backup-automation`.

## What this adds

- [scripts/production-backup.ts](../../../scripts/production-backup.ts) — the backup mechanism: env validation and fail-closed host/port/bucket/fingerprint checks, `pg_dump -Fc` via env-only libpq config (`PGSSLMODE=verify-full`, `PGCHANNELBINDING=require`, bounded `lock_timeout`), `pg_restore --list` structural validation, streaming `age` public-key encryption (official `age-encryption` JS package), R2 upload streamed from the encrypted temp file, deterministic `hourly/`/`daily/` object keys derived from an authoritative scheduled timestamp, and a single top-level `finally` that always removes both the plaintext and ciphertext temp files regardless of where the pipeline fails or succeeds.
- [tests/readiness/production-backup.ts](../../../tests/readiness/production-backup.ts) — 28 dependency-injected logic cases, a mandatory Go-`age` cross-implementation proof (F2), and a mandatory, CI-non-skippable real PostgreSQL 18 dump→encrypt→decrypt→restore proof (F3) — 30 cases in total on CI. See the F1–F7 sections below.
- [.github/workflows/production-backup.yml](../../../.github/workflows/production-backup.yml) — `workflow_dispatch`-only with a required `scheduled_at` input, `contents: read`, dedicated non-cancelling concurrency group, an activation gate (`main` + `vars.PRODUCTION_BACKUP_ACTIVATION == 'R4_APPROVED'`) that skips before any Production secret reference executes, and a job container pinned to `postgres:18@sha256:86c951e05bf56c93d95d397747fb8820ac76cc3bedb78f43abd83eedbe3666ae` (18.6, resolved live from `registry-1.docker.io` on 2026-09-21).
- [apps/backup-scheduler-worker/src/index.ts](../../../apps/backup-scheduler-worker/src/index.ts) / [wrangler.toml](../../../apps/backup-scheduler-worker/wrangler.toml) — the future Cloudflare Worker Cron scheduler: fixed dispatch constants, forwards Cloudflare's own `scheduledTime` as the `scheduled_at` input, no `fetch()` handler, never logs the dispatch token. Not deployed; `[triggers]` commented out.
- `package.json` / `scripts/verify.mjs` — `age-encryption@0.3.1` (pinned, official FiloSottile/typage), `backup:production`/`test:production-backup` scripts, wired into Foundation verify.
- `tsconfig.json` — added `apps/backup-scheduler-worker/**/*.ts` to `include`.

## R2B-F1–F7 (PR #27 review `5763014617`) — how each was addressed

- **F1 streaming encryption**: `encryptFileToFileStreaming` streams plaintext → age ciphertext file-to-file via the `age-encryption` Streams API (`Readable.toWeb`/`Writable.toWeb`, no whole-buffer `Uint8Array`). Plaintext SHA-256 is computed by a hashing `Transform` inline in the read pipeline, not a separate full read. Both the plaintext and ciphertext temp files are removed in the orchestrator's single `finally`, on every path including success. The file no longer imports `readFile` from `node:fs/promises` at all; test 27 statically asserts that import is absent, so a regression back to whole-buffer processing is caught automatically. Verified locally: a ~2.3MB smoke test and test 28's ~1.4MB round trip both match plaintext SHA-256 exactly and decrypt correctly.
- **F2 Go-age interoperability**: `tests/readiness/production-backup.ts` downloads the official `FiloSottile/age` v1.3.2 release for the running platform (Node's built-in `fetch`, no curl-pipe-shell), verifies its SHA-256 against a pinned constant before ever extracting/executing it, and proves both directions: R2B-encrypted → official `age -d` decrypts correctly, and official `age -r` encrypted → `age-encryption`'s `Decrypter` decrypts correctly. A disposable identity is generated via the official `age-keygen` for each run and never reused. **This ran for real in this session** (darwin-arm64) and passed both directions; the linux-amd64 checksum used for CI was independently re-verified byte-for-byte against `cbe24006683f8eb669266162894b9a522a1af52f2665fbc63a4bb032ed26ac10` before being pinned.
- **F3 mandatory PG18 proof, no silent CI skip**: the actual Foundation CI run on the pre-fix HEAD (`35616862960`) failed with `pg_dump exited 1` — Ubuntu 24.04's ambient `pg_dump` is not major 18, and the test used to accept "present" as good enough. The test now resolves a `PgToolRunner`: it checks the ambient tool's major version via `pg_dump --version`; if not exactly 18 it falls back to invoking the pinned `postgres:18@sha256:...` image through `docker run --rm --network host` (host networking so the container reaches the local isolated Postgres fixture), re-verifying that image's own `pg_dump --version` also reports 18 before trusting it. If `CI=true` and neither path is available, the test **throws and fails** (verified locally by re-running with `CI=true`: it now fails with `BACKUP_TEST_PG18_PROOF_UNAVAILABLE_IN_CI`, not a silent pass). Only outside CI (this local macOS sandbox, no Docker) does it SKIP, loudly. The Docker code path itself could not be exercised locally (no Docker in this sandbox) — its correctness will be confirmed by the next Foundation CI run, which is why this resubmission waits for that SUCCESS before re-posting the report.
- **F4 exact Production host identity binding**: `assertProductionHostFingerprint` compares SHA-256(lowercased, trimmed `PGHOST`) against a committed constant (`EXPECTED_PRODUCTION_HOST_FINGERPRINT_SHA256 = 7ad9939654fde65fa8bf8c4c043e33ca9053036d2137cf7616d365a11876c3fc`, as given by TD), on top of the existing shape/pooled/non-Neon checks, plus a new `assertProductionPort` requiring exactly `5432`. The expected fingerprint is a function parameter defaulting to that real constant — never sourced from an env var — so the CLI entrypoint always enforces the real value while tests can supply their own self-computed fingerprint for a synthetic host to exercise both the accept and reject paths without ever needing to know the real Production hostname (test 23). A different syntactically-valid `.neon.tech` host is proven rejected. The real fingerprint constant is encoded exactly as TD provided; it was not independently re-derived from a live Neon endpoint in this pass (no Neon credential is available, and doing so would mean discovering the real hostname, which this pass deliberately avoids).
- **F5 scheduler-authoritative timestamp**: `parseScheduledAt` strictly validates a canonical `Z`-suffixed ISO 8601 string (test 25 covers missing/malformed/offset-form rejection) and is now *required* by `runProductionBackup` (no `now()`/wall-clock fallback exists in the orchestrator at all). The Worker's `scheduled()` converts Cloudflare's `scheduledTime` (epoch ms) to that canonical ISO and sends it as the `workflow_dispatch` input `scheduled_at` (test 21); the workflow passes it through as `SCHEDULED_AT`. Because `objectKey`/daily-promotion were already pure functions of the timestamp, a retry with the same `scheduledAt` now provably reuses the same object key (test 26) — no extra retry-specific logic was needed once the authority source was fixed.
- **F6 minimal child env**: `minimalPgChildEnv` builds an explicit allow-list (`PATH`, `HOME`, `LANG`, `LC_ALL`, `TMPDIR`, `TEMP`, `TMP`) plus only the caller-supplied PG/security vars; `spawnPgDump`, `spawnPgRestoreList`, and the version probes all use it instead of `...process.env`. Test 22 proves a synthetic unrelated "secret" placed in the source env never reaches the constructed child env.
- **F7 strict BACKUP_CLASS parsing**: `parseBackupClass` accepts only the literal strings `'hourly'`/`'daily'` and throws `BACKUP_CLASS_INVALID` for anything else (including case variants); the CLI entrypoint now calls it instead of the previous `=== 'daily' ? 'daily' : 'hourly'` silent coercion. Test 13 covers this directly.

## What this deliberately does not do (still zero in this phase)

No Production DB connection, no SQL, no backup DB role, no DB password, no R2 credential, no GitHub `production-backup` environment secret, no Worker secret, no Cron activation, no Production deploy, no Square/payment/inventory/DNS action. `PRODUCTION_BACKUP_ACTIVATION` still does not exist, so the Production-secret-referencing step is architecturally unreachable until R4.

## Local verification (this environment)

- `npx tsc --noEmit`: clean.
- `npx eslint . --max-warnings=0` (full repo): clean.
- `npm run check:secrets`: PASS, 2310 files.
- `npm run check:reference`: PASS.
- `npm run test:production-backup`: **29/29 logic-level cases PASS**, plus the F2 Go-age cross-implementation proof **PASS** (real, both directions). Case F3 (real PG18 round trip) **SKIPPED**, loudly disclosed, not counted as PASS — see F3 above for why, and confirmed to correctly **FAIL instead of skip** when `CI=true` is set locally without Docker/PG18 tools.
- **Mutation test 1 (cleanup invariant)**: the orchestrator's `finally` cleanup was temporarily replaced with a no-op; `test:production-backup` failed at the plaintext-cleanup assertion; reverted from an untouched copy; 29/29 PASS reproduced.
- **Mutation test 2 (encrypted-only upload boundary)**: the upload call was temporarily changed to pass the plaintext path/size instead of the ciphertext path/size; `test:production-backup` failed exactly at test 10's "uploaded file must not be the plaintext path" assertion; reverted; 29/29 PASS reproduced.
- Full Foundation-CI-equivalent `npm run verify` (exact ubuntu-24.04 CI step list, only the pre-existing darwin-only `test:controller:macos` step excluded — unrelated to this change, per `5762567427` §3): **PASS**, evidence `.local/evidence/2026-09-21T15-44-11.356Z`, including `test:production-backup`'s 29/29 + F2 PASS and F3's expected local SKIP.

## GitHub Foundation CI

- Run `35622492817` (HEAD `ea1fa8d98ac1e42b26a2050e87104e8ef7ab1305`, the first F1–F7 fix commit): reached `test:production-backup` and is genuinely strong evidence the F1–F7 mechanism is correct on Linux — tests 1–28 all PASS, **F2's Go-age cross-implementation proof PASSED for real on Linux** (downloaded/checksum-verified `age-v1.3.2-linux-amd64.tar.gz`, both directions), and **F3's resolver correctly detected ambient `pg_dump` was not major 18 and fell back to the pinned `postgres:18` image via Docker** ("Using Docker (postgres:18@sha256:...) for the PG18 round-trip proof."), which is exactly the fallback F3 exists to exercise. It then failed with `Port 28032 is occupied; refusing to attach or kill any process` from `scripts/worktree.ts` — a test-harness bug, not a product defect: `startIsolatedPostgres()` derives its port deterministically from the worktree path, so the source `db` and the later `restoreDb` collided because the source was still running when the second instance started. Fixed by stopping the source `db` (its dump and hash already captured) before starting `restoreDb`, matching the existing stop-before-restore pattern already used in `scripts/isolated-backup.ts`.
- Run `35625021582` (HEAD `58e3e5b412543f72f17655f76ee1d3b0405d03b1`, the port-collision fix): **SUCCESS**. `test:production-backup` reported all **30/30 cases PASS** on real Linux CI: 1–28 the dependency-injected logic cases, **F2 the Go-age cross-implementation proof PASS** (checksum-verified `age-v1.3.2-linux-amd64.tar.gz`, both directions), and **F3 the mandatory PostgreSQL 18 proof PASS via the pinned `postgres:18@sha256:86c951e05bf...` image through Docker** ("Using Docker (postgres:18@sha256:...) for the PG18 round-trip proof." → "PASS F3. mandatory PostgreSQL 18 dump -> validate -> encrypt(streaming) -> decrypt -> restore proof ... reproduces identical rows"). The full Foundation job (every other existing test command plus this one, then `npm run runner:dry-run`) also finished SUCCESS.
